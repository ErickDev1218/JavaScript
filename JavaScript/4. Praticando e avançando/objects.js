//Object
//ATENÇÃO NA SINTAXE

const person = {
    name: 'John',
    age: 30,
    weight: 103,
    isAdmin: true
}

//Importante usar o 'person.' antes para que busque dentro do objeto.
console.log(`${person.name} tem ${person.age} anos`)